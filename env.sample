# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=botcraft_db

# JWT Secret
JWT_SECRET=your_jwt_secret_key_should_be_long_and_random

# Email Configuration (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# Application
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NODE_ENV=development

# Note: For production, use more secure values and store them in a secure environment
# For Gmail, you need to use an App Password if you have 2FA enabled
# https://support.google.com/accounts/answer/185833?hl=en
